# 🤖 UptimeRobot 配置教程

## 防止 Render 服务器休眠的完整指南

---

## 📋 目录

1. [为什么需要 UptimeRobot？](#为什么需要-uptimerobot)
2. [注册 UptimeRobot](#步骤-1注册-uptimerobot)
3. [创建监控](#步骤-2创建监控)
4. [验证配置](#步骤-3验证配置)
5. [查看监控状态](#步骤-4查看监控状态)
6. [常见问题](#常见问题)

---

## 🎯 为什么需要 UptimeRobot？

**Render 免费套餐的限制**：
- ⚠️ 15 分钟无活动后自动休眠
- ⚠️ 下次访问需要等待 30-60 秒冷启动

**UptimeRobot 的作用**：
- ✅ 每 5 分钟自动访问你的服务器
- ✅ 保持服务器一直处于唤醒状态
- ✅ 玩家访问时秒进，无需等待
- ✅ 完全免费（最多 50 个监控）

**原理图解**：
```
没有 UptimeRobot:
15分钟 → 休眠 💤 → 用户访问 → 等待30秒 😴 → 连接成功

有了 UptimeRobot:
每5分钟 ping → 保持唤醒 ⚡ → 用户访问 → 立即连接 ✅
```

---

## 🚀 步骤 1：注册 UptimeRobot

### 1.1 访问官网

打开浏览器，访问：
```
https://uptimerobot.com
```

### 1.2 点击注册

在首页点击：
- **"Sign Up Free"** 按钮
- 或右上角的 **"Sign Up"**

### 1.3 选择注册方式

**方式一：使用 Google 账号**（推荐，最快）
- 点击 **"Sign up with Google"**
- 选择你的 Google 账号
- 授权登录

**方式二：使用邮箱注册**
1. 输入邮箱地址
2. 设置密码（至少 8 个字符）
3. 点击 **"Sign Up"**
4. 打开邮箱，点击验证链接

### 1.4 完成注册

注册成功后会自动跳转到 Dashboard（控制面板）

---

## ⚙️ 步骤 2：创建监控

### 2.1 进入添加监控页面

在 Dashboard 页面：
1. 点击左侧菜单的 **"Add New Monitor"**
2. 或点击页面中间的 **"+ Add New Monitor"** 按钮

### 2.2 配置监控设置

**重要：按照以下设置填写** ⚠️

#### 📌 Monitor Type（监控类型）
```
选择: HTTP(s)
```
> 这是监控网站和 API 的标准类型

#### 📌 Friendly Name（监控名称）
```
输入: Couple Game Server
```
> 或者任何你喜欢的名字，方便识别

#### 📌 URL (or IP)（要监控的地址）

**⚠️ 重要：填写你的 Render 服务器地址**

格式：
```
https://你的服务名.onrender.com/health
```

示例：
```
https://couple-game-server.onrender.com/health
```

> **为什么用 `/health` 端点？**
> - 轻量级，不占用太多资源
> - 专门用于健康检查
> - 响应快速

#### 📌 Monitoring Interval（监控间隔）

**免费套餐选项**：
```
选择: 5 minutes
```

**其他选项**（付费）：
- 1 minute（每分钟一次）
- 2 minutes
- 3 minutes

> **为什么选 5 分钟？**
> - 免费套餐最短间隔
> - 远小于 15 分钟休眠时间
> - 足够保持服务器唤醒

#### 📌 Monitor Timeout（超时时间）

```
保持默认: 30 seconds
```

#### 📌 Alert Contacts（告警联系人）

```
可选: 勾选你的邮箱
```

> 如果服务器宕机会发邮件通知你

### 2.3 完整配置示例

```
┌─────────────────────────────────────────┐
│ Monitor Type: HTTP(s)                   │
│ Friendly Name: Couple Game Server       │
│ URL: https://couple-game-server.on...  │
│      /health                            │
│ Monitoring Interval: Every 5 minutes    │
│ Monitor Timeout: 30 seconds             │
│ Alert Contacts: ☑ your@email.com       │
└─────────────────────────────────────────┘
```

### 2.4 创建监控

检查配置无误后：
1. 点击页面底部的 **"Create Monitor"** 绿色按钮
2. 等待几秒钟

**成功提示**：
```
✅ Monitor has been created successfully!
```

---

## ✅ 步骤 3：验证配置

### 3.1 查看监控状态

创建后，你会看到监控列表：

```
┌─────────────────────────────────────────────────────────┐
│ Couple Game Server                                      │
│ https://couple-game-server.onrender.com/health         │
│ Status: Up ✅          Uptime: 100%                     │
│ Last Check: 1 minute ago                                │
└─────────────────────────────────────────────────────────┘
```

### 3.2 检查首次 Ping

点击监控名称 **"Couple Game Server"** 查看详情：

**应该看到**：
- ✅ **Status**: Up（在线）
- ✅ **Response Time**: 100-500ms
- ✅ **Last Checked**: 刚才的时间

**如果显示 Down（离线）**：
1. 检查 URL 是否正确
2. 确认 Render 服务器已部署成功
3. 在浏览器直接访问 URL 测试
4. 查看 Render 日志是否有错误

### 3.3 测试服务器保持唤醒

**测试方法**：
1. 等待 10 分钟不访问服务器
2. 然后打开游戏或访问服务器 URL
3. 应该立即连接成功，无需等待 ⚡

**对比**：
- ❌ **没有 UptimeRobot**：等待 30-60 秒
- ✅ **有了 UptimeRobot**：立即连接（< 1 秒）

---

## 📊 步骤 4：查看监控状态

### 4.1 Dashboard 总览

在主页面可以看到：
- **Uptime**（在线率）：应该接近 100%
- **Response Time**（响应时间）：平均响应速度
- **Status**：当前状态（Up/Down）

### 4.2 详细统计

点击监控名称，查看：

**最近 24 小时**：
```
┌──────────────────────────────────┐
│ Response Times (24 hours)        │
│ ████████████████████             │
│ Avg: 250ms  Min: 120ms Max: 450ms
└──────────────────────────────────┘
```

**最近 7 天**：
- Uptime: 99.98%
- Total Checks: 2,016
- Down Times: 0

**响应时间图表**：
- 可以看到每次 ping 的响应时间
- 观察服务器性能趋势

### 4.3 告警记录

如果服务器曾经宕机：
- 会显示在 **"Down Times"** 列表
- 包含宕机时间、恢复时间、宕机原因

---

## 🔔 配置告警（可选）

### 4.1 添加告警联系人

1. 点击左侧菜单 **"Alert Contacts"**
2. 点击 **"Add Alert Contact"**
3. 选择通知方式：
   - ✉️ **Email**（邮件，免费）
   - 📱 **SMS**（短信，付费）
   - 💬 **Telegram**（推荐，免费）
   - 🔔 **Slack**、**Discord** 等

### 4.2 配置 Telegram 通知（推荐）

**步骤**：
1. 在 Telegram 搜索 `@uptimerobbot`
2. 发送 `/start` 开始
3. Bot 会返回一个 **Chat ID**
4. 在 UptimeRobot 中添加 Telegram 联系人
5. 输入 Chat ID

**收到的通知示例**：
```
⚠️ Couple Game Server is DOWN
URL: https://couple-game-server.onrender.com/health
Reason: Connection timeout
Time: 2024-11-12 15:30:00

✅ Couple Game Server is UP again
Downtime: 2 minutes 30 seconds
```

---

## 🎛️ 高级配置（可选）

### 自定义监控间隔

**如果升级到付费套餐**（$7/月）：
- 可以设置 1 分钟间隔
- 更快响应服务器问题
- 但免费的 5 分钟已经足够

### 添加多个监控

**如果有多个服务**：
```
监控 1: 游戏服务器
  https://couple-game-server.onrender.com/health

监控 2: Web 前端
  https://qq.cpflying.top

监控 3: API 服务
  https://api.yourservice.com/health
```

### 状态页面（Status Page）

**创建公开状态页面**：
1. 点击 **"Public Status Pages"**
2. 创建状态页面
3. 玩家可以查看服务器在线状态

---

## ❓ 常见问题

### Q1: 免费套餐有什么限制？

**免费套餐包含**：
- ✅ 最多 50 个监控
- ✅ 5 分钟监控间隔
- ✅ 邮件告警
- ✅ 2 个月数据保留

**对你来说完全够用！** 🎉

### Q2: 会不会浪费 Render 的 750 小时额度？

**不会！**
- UptimeRobot 的 ping 不计入使用时间
- 只是保持服务器唤醒
- 750 小时足够一个月 24/7 运行（720 小时）

### Q3: 服务器显示 Down 怎么办？

**排查步骤**：
1. 浏览器直接访问 URL 测试
2. 检查 Render Dashboard 服务状态
3. 查看 Render 日志是否有错误
4. 确认 `/health` 端点正常响应
5. 检查 URL 拼写是否正确

### Q4: 可以监控 IP 地址吗？

**不推荐**：
- Render 的 IP 会变化
- 应该使用域名（.onrender.com）

### Q5: 监控间隔可以更短吗？

**免费套餐**：
- 最短 5 分钟
- 已经足够防止 15 分钟休眠

**付费套餐**（$7/月）：
- 可以 1 分钟
- 但对防休眠来说没必要

### Q6: 会影响服务器性能吗？

**不会**：
- `/health` 端点非常轻量
- 只返回简单的 JSON
- 几乎零性能开销
- 每月只有约 8,640 次请求（5分钟 × 30天）

---

## 📱 移动端查看（可选）

### iOS / Android App

UptimeRobot 有官方移动应用：

**功能**：
- 📊 查看所有监控状态
- 🔔 接收推送通知
- 📈 查看统计图表
- ⚙️ 管理监控设置

**下载**：
- iOS: App Store 搜索 "UptimeRobot"
- Android: Google Play 搜索 "UptimeRobot"

---

## ✅ 完成检查清单

设置完成后，确认以下几点：

- [ ] ✅ 已注册 UptimeRobot 账号
- [ ] ✅ 创建了监控
- [ ] ✅ URL 格式正确：`https://xxx.onrender.com/health`
- [ ] ✅ 监控间隔设置为 5 分钟
- [ ] ✅ 监控状态显示 **Up**（在线）
- [ ] ✅ 配置了邮件告警
- [ ] ✅ 测试服务器保持唤醒

---

## 🎉 恭喜完成配置！

**现在你的服务器将会**：
- ✅ 24/7 保持唤醒
- ✅ 玩家访问秒进
- ✅ 无需等待冷启动
- ✅ 完全免费运行

**监控效果**：
```
📊 UptimeRobot 统计（7天后）

Uptime: 99.99%
Total Checks: 2,016
Response Time: Avg 250ms
Down Times: 0

🎮 玩家体验：立即连接 ⚡
```

---

## 🔗 相关链接

- [UptimeRobot 官网](https://uptimerobot.com)
- [UptimeRobot 文档](https://uptimerobot.com/help/)
- [Render 部署指南](./DEPLOYMENT.md)

---

**配置愉快！** 🚀✨

现在你的游戏服务器已经完美运行了，享受和另一半的游戏时光吧！🎮💕
